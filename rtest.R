names(help)




  help %>% group_by(x) %>% summarise(exp(mean(log_rf)),exp(sd(log_rf)))
  help %>% group_by(x) %>% summarise(exp(mean(log_rf)),exp(sd(log_scl70_1)))
  help %>% group_by(x) %>% summarise(exp(mean(log_rf)),exp(sd(log_sm1)))
  help %>% group_by(x) %>% summarise(exp(mean(log_rf)),exp(sd(log_ssb1)))
  help %>% group_by(x) %>% summarise(exp(mean(log_rf)),exp(sd(log_smrnp1)))
  help %>% group_by(x) %>% summarise(exp(mean(log_rf)),exp(sd(log_ssa)))
  help %>% group_by(x) %>% summarise(exp(mean(log_rf)),exp(sd(log_ana)))
  help %>% group_by(x) %>% summarise(exp(mean(log_rf)),exp(sd(log_anca)))




1*(1.1)^30
  
  






